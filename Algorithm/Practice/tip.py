# 🔹 인덱스(arr[0])를 기준으로 시작해야 하는 경우
# - 최대값 / 최소값처럼 "비교 기준"이 필요한 경우
# - 데이터 안에 실제로 존재하는 값으로 시작해야 안전함
# - 음수/양수 섞여 있어도 오류 없음

# 🔹 인덱스를 쓰면 안 되는 경우
# 1) 누적용 변수일 때 (합, 개수)
#    → total = 0, count = 0
#
# 2) 상태 표시용 변수일 때 (찾았다 / 못 찾았다)
#    → found = False, idx = -1
#
# 3) 리스트가 아닐 때 (단일 값)
#    → 인덱스 개념 없음
#
# 4) 리스트가 비어 있을 가능성이 있을 때
#    → arr[0] 접근 시 에러 발생

# 🔹 판단 기준 한 줄 정리
# "이 변수가 비교 기준인가?"
#   YES → arr[0]
#   NO  → 0 또는 False

##########################################################################################

#                               문장 → 코드 치환표
# | 문제 문장 표현          | 머릿속 번역            | 코드 반응                    |
# | --------------         | -----------            | ------------------------    |
# | “각 원소에 대해”        | 기준을 하나씩 잡아라    | `for i in range(...)`       |
# | “오른쪽 / 이후”         | 기준 다음부터 본다      | `for j in range(i+1, N)`    |
# | “왼쪽 / 이전”           | 기준 이전만 본다        | `for j in range(0, i)`      |
# | “~보다 크면 / 작으면”   | 조건 비교               | `if A[i] > A[j]`            |
# | “개수”                 | 센다                    | `count += 1`                |
# | “가장 큰 / 가장 작은”   | 갱신                    | `max_val = max(...)`        |


# 각 숫자에 대해, 자기보다 오른쪽에 있으면서 더 작은 숫자의 개수
# 각 숫자 → for i
# 오른쪽 → for j = i+1
# 더 작으면 → if >
# 개수 → count

# 배열에서 각 위치를 기준으로, 이후에 나오는 값 중 조건을 만족하는 개수의 최댓값
# 각 위치 → for i
# 이후 → for j = i+1
# 개수 → count
# 최댓값 → max 갱신

# 어떤 원소보다 뒤에 있으면서 값이 더 큰 원소의 수
# 기준 → for i
# 뒤에 → for j = i+1
# 더 크면 → if A[j] > A[i]
# 센다 → count