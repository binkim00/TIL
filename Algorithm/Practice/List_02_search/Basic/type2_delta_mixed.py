####################################################
# [유형 2] 델타 탐색 (난이도 혼합)
#
# N x N 배열에서 각 칸을 중심으로
# 상하좌우 + 대각선의 합을 구한다.
# (자기 자신 포함)
# 그 중 최댓값을 출력하시오.
####################################################

T = int(input())
for tc in range(1, T + 1):
    N = int(input())
    arr = [list(map(int, input().split())) for _ in range(N)]

    max_sum = 0

    di = [-1, 1, 0, 0, -1, -1, 1, 1]
    dj = [0, 0, -1, 1, -1, 1, -1, 1]

    # TODO: 델타 탐색으로 합 계산

    print(f"#{tc} {max_sum}")
