문자열 입력
s = input()

정수 입력 기본
n = int(input())

두 개 입력
a, b = map(int, input().split())

여러 개 입력
A, B, C = map(int, input().split())

arr = list(map(int, input().split()))
A = arr[0]
B = arr[1]
C = arr[2]

수정사항